	2026-01-06 14:14

Status: [[Jazz/3 - Tags/t-child]]

Tags: [[Jazz/3 - Tags/t-project]] [[Jazz/3 - Tags/t-programming]] [[Jazz/3 - Tags/t-webdev]] [[Jazz/3 - Tags/t-PortWebLogicOrCode]]

---
# 5.2 Discord user id copy - Code Snippet; Logic

---

## ✅ Convert THIS into Copy-to-Clipboard (Best Way)

### 🔹 Updated HTML (minimal change)

```html
<a href="#"
   class="home__link"
   onclick="copyDiscord(event)">
  <i class="ri-discord-fill"></i>
</a>

<span id="discord-msg" class="copy-msg"></span>
```

---

## 🧠 JavaScript (add once in your JS file)

```js
function copyDiscord(event) {
  event.preventDefault(); // stops page jump

  const discordUsername = "YourUsername#1234";
  const msg = document.getElementById("discord-msg");

  navigator.clipboard.writeText(discordUsername).then(() => {
    msg.textContent = "Discord username copied!";
    
    setTimeout(() => {
      msg.textContent = "";
    }, 2000);
  });
}
```

🔁 Replace `YourUsername#1234` with your actual Discord username.

---

## 🎨 Optional CSS (clean feedback text)

```css
.copy-msg {
  display: block;
  margin-top: 0.5rem;
  font-size: 0.85rem;
  color: var(--first-color);
}
```

---

## ⭐ Best UX Upgrade (Tooltip on Hover)

```html
<a href="#"
   class="home__link"
   onclick="copyDiscord(event)"
   title="Copy Discord username">
  <i class="ri-discord-fill"></i>
</a>
```

Simple. Professional. Clean.

---

This line creates a **placeholder element** that JavaScript can update dynamically.

```html
<span id="discord-msg" class="copy-msg"></span>
```

## 🧠 What This Element Is

`<span>` is an **inline HTML element** used to hold **small pieces of content**, usually text.

By itself, this span:

- Shows **nothing**
    
- Takes **no visible space**
    
- Just exists in the DOM

Think of it as an **empty label waiting to be filled**.

---

## 🔍 Part-by-Part Explanation

### 1️⃣ `<span> ... </span>`

- Inline container
    
- Does **not** break lines (unlike `<div>`)
    
- Commonly used for:
    
    - Messages
        
    - Icons
        
    - Small dynamic text

---

### 2️⃣ `id="discord-msg"`

- Gives the element a **unique identifier**
    
- Allows JavaScript to find it:
    

```js
document.getElementById("discord-msg");
```

This is how JS knows **where to show the message**.

---

### 3️⃣ `class="copy-msg"`

- Used for **styling with CSS**
    

Example:

```css
.copy-msg {
  font-size: 0.85rem;
  color: var(--first-color);
}
```

ID = JavaScript  
Class = CSS

(That separation is best practice.)

---

## 🔁 How It’s Used in Your Code

### JavaScript fills it dynamically:

```js
msg.textContent = "Discord username copied!";
```

Result in the browser:

```html
<span>Discord username copied!</span>
```

After a timeout, JS clears it:

```js
msg.textContent = "";
```

The message disappears again.

---

## 🧠 Why This Is Better Than `alert()`

✔ Non-blocking  
✔ Looks professional  
✔ No popup interruptions  
✔ Works well on mobile

This is **real-world UI feedback**.

---

## 🎯 Interview-Ready Explanation

> “This span acts as a placeholder for dynamic feedback text that JavaScript updates after the user action.”

---

# Updated Code

Instead of text popping up upon copying the user id. The discord logo now changes to link logo for some amount of time.

## 🎯 Goal (Clear)

When the Discord icon is clicked:

1. Username is copied ✅
    
2. Discord icon changes to **link icon** `<i class="ri-link"></i>` 🔁
    
3. After a short duration (e.g. 1.5s), it switches back 🔄

---

## ✅ Final Working Solution

### 1️⃣ HTML (very small change)

Give the `<i>` an `id` so JS can target it.

```html
<a href="#"
   class="home__link"
   onclick="copyDiscord(event)">
  <i id="discord-icon" class="ri-discord-fill"></i>
</a>
```

---

### 2️⃣ JavaScript (clean & readable)

```js
function copyDiscord(event) {
  event.preventDefault();

  const discordUsername = "YourUsername#1234";
  const icon = document.getElementById("discord-icon");

  navigator.clipboard.writeText(discordUsername).then(() => {
    // change icon
    icon.classList.remove("ri-discord-fill");
    icon.classList.add("ri-link");

    // revert after 1.5 seconds
    setTimeout(() => {
      icon.classList.remove("ri-link");
      icon.classList.add("ri-discord-fill");
    }, 1500);
  });
}
```

🔁 Replace `YourUsername#1234` with your real Discord username.

---
# Reference
